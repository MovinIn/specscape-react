# Optional: override the Vite /api proxy target (default: https://electrosense.org)
# VITE_API_PROXY_TARGET=http://localhost:8080

# Force mock API (also auto-falls back in DEV when the live API returns HTML/errors)
# VITE_USE_MOCK_API=true
